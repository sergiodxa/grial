{
  "name": "grial-example-jsonplaceholder-wrapper",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:report": "jest --coverage",
    "start": "grial start",
    "alias": "now alias",
    "predeploy": "npm test",
    "deploy": "now",
    "postdeploy": "npm run alias"
  },
  "dependencies": {
    "@grial/cli": "^1.3.0",
    "@grial/connector-rest": "^1.0.1",
    "@grial/server": "^1.4.0",
    "dataloader": "^1.3.0"
  },
  "devDependencies": {
    "jest": "^20.0.4"
  },
  "jest": {
    "bail": true,
    "notify": true,
    "verbose": true
  }
}
